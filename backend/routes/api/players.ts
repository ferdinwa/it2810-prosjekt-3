/*

const router = express.Router();

// Player Model
const Player = require('../../models/player.ts')

// @route GET api/players
// @desc GET all items
// @access Public
router.get('/', (req: Request, res: Response) => {
    Player.find()
        .sort({ age: -1 })
        .then((players : string) => res.json(players))
});

module.exports = router;

*/